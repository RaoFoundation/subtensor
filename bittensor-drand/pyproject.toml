[project]
name = "bittensor-drand"
# Static (not read from Cargo.toml) so the release train can stamp PEP 440
# rc/dev suffixes that cargo's semver would reject.
version = "2.0.0"
description = "Rust-backed Python library for generating timelock-encrypted weight commitments for Bittensor's commit-reveal mechanism using drand randomness."
readme = "README.md"
license = {file = "LICENSE"}
keywords = ["substrate", "scale", "codec", "bittensor", "commit reveal", "drand", "TLE"]

dependencies = []
requires-python = ">= 3.9"

authors = [
  {name = "Roman Chkhaidze", email = "r@latent.to"},
]
maintainers = [
  {name = "Cortex Team", email = "cortex@latent.to"},
]
classifiers = [
        "Development Status :: 5 - Production/Stable",
        "Intended Audience :: Developers",
        "Topic :: Software Development :: Build Tools",
        "License :: OSI Approved :: MIT License",
        "Programming Language :: Python :: 3 :: Only",
        "Programming Language :: Python :: 3.10",
        "Programming Language :: Python :: 3.11",
        "Programming Language :: Python :: 3.12",
        "Programming Language :: Python :: 3.13",
        "Programming Language :: Python :: 3.14",
        "Topic :: Scientific/Engineering",
        "Topic :: Scientific/Engineering :: Mathematics",
        "Topic :: Scientific/Engineering :: Artificial Intelligence",
        "Topic :: Software Development",
        "Topic :: Software Development :: Libraries",
        "Topic :: Software Development :: Libraries :: Python Modules",
]

[project.urls]
Repository = "https://github.com/RaoFoundation/subtensor"

[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[tool.maturin]
include = ["bittensor_drand*", "src*"]
exclude = ["tests*"]
# Enable pyo3's extension-module on the dependency directly instead of via a
# crate feature, so `cargo test --workspace --all-features` doesn't turn it on
# (it disables libpython linking, which breaks test-binary links).
features = ["pyo3/extension-module"]

[project.optional-dependencies]
dev = [
    "maturin==1.7.0",
    "pytest-asyncio==0.23.7"
]
