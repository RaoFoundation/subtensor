---
title: "subnets"
description: "Info for every subnet, fetched in four batched map queries rather than"
---

{/* GENERATED by scripts/generate.py from the SDK registries — do not edit. */}

one-query-per-subnet. This is what listing should use.

**Category:** Subnets

## Parameters

This read takes no parameters.

## CLI

```bash
btcli query subnets --json
```

## Python

Namespace method (autocomplete, signature help):

```python
import bittensor as bt

sub = bt.Subtensor()
result = sub.subnets.subnets()
```

Or dispatch by name, as an agent would:

```python
result = sub.read("subnets")
```

Async is the same surface awaited: `async with bt.Subtensor() as client:`.

## On-chain implementation

- Storage [`SubtensorModule.NetworksAdded`](/code/pallets/subtensor/src/lib.rs#L2174)
- Storage [`SubtensorModule.Tempo`](/code/pallets/subtensor/src/lib.rs#L2111)
- Storage [`SubtensorModule.Burn`](/code/pallets/subtensor/src/lib.rs#L2407)
- Storage [`SubtensorModule.SubnetworkN`](/code/pallets/subtensor/src/lib.rs#L2170)

Every file is browsable under [/code](/code) exactly as built into the runtime.
