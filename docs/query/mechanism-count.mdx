---
title: "mechanism-count"
description: "Current mechanism count configured for a subnet."
---

{/* GENERATED by scripts/generate.py from the SDK registries — do not edit. */}



**Category:** Subnets

## Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `netuid` | integer | Subnet to query. |

## CLI

```bash
btcli query mechanism-count --netuid <integer> --json
```

## Python

Typed namespace method (autocomplete, signature help):

```python
import bittensor as sub

async with sub.Client("finney") as client:
    result = await client.subnets.mechanism_count(netuid=1)
```

Or dispatch by name, as an agent would:

```python
result = await client.read("mechanism_count", netuid=1)
```
