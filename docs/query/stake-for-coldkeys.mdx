---
title: "stake-for-coldkeys"
description: "Every stake position for several coldkeys at once, in one runtime call."
---

{/* GENERATED by scripts/generate.py from the SDK registries — do not edit. */}

Each position's `stake` is denominated in the subnet's own currency: subnet
alpha, or TAO when netuid is 0.

**Category:** Staking

## Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `coldkey_ss58s` | array | Coldkeys whose stake positions to list. |

## CLI

```bash
btcli query stake-for-coldkeys --coldkey-ss58s <array> --json
```

## Python

```python
import bittensor as sub

async with sub.Client("finney") as client:
    result = await client.read("stake_for_coldkeys", coldkey_ss58s=[...])
```
