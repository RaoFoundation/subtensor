---
title: "root-claim-type"
description: "How a coldkey claims root alpha emission: Swap, Keep, or KeepSubnets(+subnets)."
---

{/* GENERATED by scripts/generate.py from the SDK registries — do not edit. */}



**Category:** Staking

## Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `coldkey_ss58` | string | Coldkey whose root-claim setting to read. |

## CLI

```bash
btcli query root-claim-type --coldkey <ss58|name> --json
```

## Python

Namespace method (autocomplete, signature help):

```python
import bittensor as bt

sub = bt.Subtensor()
result = sub.staking.root_claim_type(coldkey_ss58="5F...")
```

Or dispatch by name, as an agent would:

```python
result = sub.read("root_claim_type", coldkey_ss58="5F...")
```

Async is the same surface awaited: `async with bt.Subtensor() as client:`.

## On-chain implementation

- Storage [`SubtensorModule.RootClaimType`](/code/pallets/subtensor/src/lib.rs#L2712)

Every file is browsable under [/code](/code) exactly as built into the runtime.
