---
title: "subnet-identity"
description: "The identity metadata of a subnet, or None."
---

{/* GENERATED by scripts/generate.py from the SDK registries — do not edit. */}



**Category:** Subnets

## Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `netuid` | integer | Subnet whose identity to read. |

## CLI

```bash
btcli query subnet-identity --netuid <integer> --json
```

## Python

Typed namespace method (autocomplete, signature help):

```python
import bittensor as sub

async with sub.Client("finney") as client:
    result = await client.subnets.subnet_identity(netuid=1)
```

Or dispatch by name, as an agent would:

```python
result = await client.read("subnet_identity", netuid=1)
```
