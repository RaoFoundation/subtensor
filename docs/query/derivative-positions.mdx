---
title: "derivative-positions"
description: "Every open long and short a coldkey holds, across all subnets."
---

{/* GENERATED by scripts/generate.py from the SDK registries — do not edit. */}



**Category:** Prices & swaps

## Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `coldkey_ss58` | string | Coldkey whose positions to list. |

## CLI

```bash
btcli query derivative-positions --coldkey <ss58|name> --json
```

## Python

Namespace method (autocomplete, signature help):

```python
import bittensor as bt

sub = bt.Subtensor()
result = sub.prices.derivative_positions(coldkey_ss58="5F...")
```

Or dispatch by name, as an agent would:

```python
result = sub.read("derivative_positions", coldkey_ss58="5F...")
```

Async is the same surface awaited: `async with bt.Subtensor() as client:`.

## On-chain implementation

- Storage [`Derivatives.Positions`](/code/pallets/derivatives/src/lib.rs#L82)

Every file is browsable under [/code](/code) exactly as built into the runtime.
