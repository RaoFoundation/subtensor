---
title: "stake-value-for-coldkeys"
description: "Spot-price stake valuation for several coldkeys at once, at one block."
---

{/* GENERATED by scripts/generate.py from the SDK registries — do not edit. */}



**Category:** Staking

## Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `coldkey_ss58s` | array | Coldkeys whose stake to value. |

## CLI

```bash
btcli query stake-value-for-coldkeys --coldkeys <array> --json
```

## Python

Typed namespace method (autocomplete, signature help):

```python
import bittensor as sub

async with sub.Client("finney") as client:
    result = await client.staking.stake_value_for_coldkeys(coldkey_ss58s=[...])
```

Or dispatch by name, as an agent would:

```python
result = await client.read("stake_value_for_coldkeys", coldkey_ss58s=[...])
```
