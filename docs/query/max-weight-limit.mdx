---
title: "max-weight-limit"
description: "Normalized-fraction cap on any single weight: after the chain normalizes the submitted vector, no weight may exceed max_weight_limit/65535 of the total. Not a raw u16 ceiling per weight."
---

{/* GENERATED by scripts/generate.py from the SDK registries — do not edit. */}



**Category:** Hyperparameters

## Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `netuid` | integer | Subnet to query. |

## CLI

```bash
btcli query max-weight-limit --netuid <integer> --json
```

## Python

Namespace method (autocomplete, signature help):

```python
import bittensor as bt

sub = bt.Subtensor()
result = sub.hyperparameters.max_weight_limit(netuid=1)
```

Or dispatch by name, as an agent would:

```python
result = sub.read("max_weight_limit", netuid=1)
```

Async is the same surface awaited: `async with bt.Subtensor() as client:`.

## On-chain implementation

- Storage [`SubtensorModule.MaxWeightsLimit`](/code/pallets/subtensor/src/lib.rs#L2216)

Every file is browsable under [/code](/code) exactly as built into the runtime.
