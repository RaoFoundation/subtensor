---
title: "most-convicted-hotkey"
description: "Hotkey with the highest conviction on a subnet, if any."
---

{/* GENERATED by scripts/generate.py from the SDK registries — do not edit. */}



**Category:** Locks & conviction

## Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `netuid` | integer | Subnet to query. |

## CLI

```bash
btcli query most-convicted-hotkey --netuid <integer> --json
```

## Python

Typed namespace method (autocomplete, signature help):

```python
import bittensor as sub

async with sub.Client("finney") as client:
    result = await client.locks.most_convicted_hotkey(netuid=1)
```

Or dispatch by name, as an agent would:

```python
result = await client.read("most_convicted_hotkey", netuid=1)
```
