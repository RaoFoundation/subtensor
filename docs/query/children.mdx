---
title: "children"
description: "Child hotkeys of a parent on a subnet, as (proportion, child_ss58) pairs."
---

{/* GENERATED by scripts/generate.py from the SDK registries — do not edit. */}

Proportions are u64-normalized fractions of the parent's stake, where
u64::MAX means 100%. This is the stored relation set; while the parent is
below the childkey stake threshold (see `childkey_threshold_suspended`)
the chain treats these edges as inert on every subnet the parent does
not own.

**Category:** Delegation

## Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `hotkey_ss58` | string | Parent hotkey whose children to list. |
| `netuid` | integer | Subnet to query. |

## CLI

```bash
btcli query children --hotkey <ss58|name> --netuid <integer> --json
```

## Python

Namespace method (autocomplete, signature help):

```python
import bittensor as bt

sub = bt.Subtensor()
result = sub.delegation.children(hotkey_ss58="5F...", netuid=1)
```

Or dispatch by name, as an agent would:

```python
result = sub.read("children", hotkey_ss58="5F...", netuid=1)
```

Async is the same surface awaited: `async with bt.Subtensor() as client:`.

## On-chain implementation

- Storage [`SubtensorModule.ChildKeys`](/code/pallets/subtensor/src/lib.rs#L1580)

Every file is browsable under [/code](/code) exactly as built into the runtime.
