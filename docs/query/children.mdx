---
title: "children"
description: "Child hotkeys of a parent on a subnet, as (proportion, child_ss58) pairs."
---

{/* GENERATED by scripts/generate.py from the SDK registries — do not edit. */}

Proportions are u64-normalized fractions of the parent's stake, where
u64::MAX means 100%.

**Category:** Delegation

## Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `hotkey_ss58` | string | Parent hotkey whose children to list. |
| `netuid` | integer | Subnet to query. |

## CLI

```bash
btcli query children --hotkey <ss58|name> --netuid <integer> --json
```

## Python

```python
import bittensor as sub

async with sub.Client("finney") as client:
    result = await client.read("children", hotkey_ss58="5F...", netuid=1)
```
