---
title: "proxies"
description: "Proxy delegations of an account: who may sign on its behalf, and the reserved deposit."
---

{/* GENERATED by scripts/generate.py from the SDK registries — do not edit. */}



**Category:** Accounts & keys

## Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `coldkey_ss58` | string | Account whose proxy delegations to list. |

## CLI

```bash
btcli query proxies --coldkey <ss58|name> --json
```

## Python

Namespace method (autocomplete, signature help):

```python
import bittensor as bt

sub = bt.Subtensor()
result = sub.balances.proxies(coldkey_ss58="5F...")
```

Or dispatch by name, as an agent would:

```python
result = sub.read("proxies", coldkey_ss58="5F...")
```

Async is the same surface awaited: `async with bt.Subtensor() as client:`.

## On-chain implementation

- Storage [`Proxy.Proxies`](/code/pallets/proxy/src/lib.rs#L830)

Every file is browsable under [/code](/code) exactly as built into the runtime.
