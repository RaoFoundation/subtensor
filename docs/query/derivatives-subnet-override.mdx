---
title: "derivatives-subnet-override"
description: "Root-set per-subnet overrides of the derivatives parameters, or None."
---

{/* GENERATED by scripts/generate.py from the SDK registries — do not edit. */}

None means the subnet runs on the global `derivatives_params`. When set,
`shorts_enabled` and `longs_enabled` replace the global switches for opens
on this subnet, and `max_pool_share` replaces the global cap when it is not
None. Open positions are unaffected: a paused side can still close.

**Category:** Prices & swaps

## Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `netuid` | integer | Subnet to look up. |

## CLI

```bash
btcli query derivatives-subnet-override --netuid <integer> --json
```

## Python

Namespace method (autocomplete, signature help):

```python
import bittensor as bt

sub = bt.Subtensor()
result = sub.prices.derivatives_subnet_override(netuid=1)
```

Or dispatch by name, as an agent would:

```python
result = sub.read("derivatives_subnet_override", netuid=1)
```

Async is the same surface awaited: `async with bt.Subtensor() as client:`.

## On-chain implementation

- Storage [`Derivatives.SubnetOverrides`](/code/pallets/derivatives/src/lib.rs#L133)

Every file is browsable under [/code](/code) exactly as built into the runtime.
