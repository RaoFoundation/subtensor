---
title: "locks-for-coldkey"
description: "Every lock a coldkey holds (one per subnet), rolled forward to now."
---

{/* GENERATED by scripts/generate.py from the SDK registries — do not edit. */}



**Category:** Locks & conviction

## Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `coldkey_ss58` | string | Coldkey whose locks to list. |

## CLI

```bash
btcli query locks-for-coldkey --coldkey <ss58|name> --json
```

## Python

Namespace method (autocomplete, signature help):

```python
import bittensor as bt

sub = bt.Subtensor()
result = sub.locks.locks_for_coldkey(coldkey_ss58="5F...")
```

Or dispatch by name, as an agent would:

```python
result = sub.read("locks_for_coldkey", coldkey_ss58="5F...")
```

Async is the same surface awaited: `async with bt.Subtensor() as client:`.

## On-chain implementation

- Storage [`SubtensorModule.Lock`](/code/pallets/subtensor/src/lib.rs#L1567)

Every file is browsable under [/code](/code) exactly as built into the runtime.
