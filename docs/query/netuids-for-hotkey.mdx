---
title: "netuids-for-hotkey"
description: "Every subnet a hotkey is registered on, as a sorted list of netuids."
---

{/* GENERATED by scripts/generate.py from the SDK registries — do not edit. */}



**Category:** Neurons & registration

## Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `hotkey_ss58` | string | Hotkey whose subnet registrations to list. |

## CLI

```bash
btcli query netuids-for-hotkey --hotkey <ss58|name> --json
```

## Python

Typed namespace method (autocomplete, signature help):

```python
import bittensor as sub

async with sub.Client("finney") as client:
    result = await client.neurons.netuids_for_hotkey(hotkey_ss58="5F...")
```

Or dispatch by name, as an agent would:

```python
result = await client.read("netuids_for_hotkey", hotkey_ss58="5F...")
```
