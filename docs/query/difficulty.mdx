---
title: "difficulty"
description: "Current PoW registration difficulty for a subnet."
---

{/* GENERATED by scripts/generate.py from the SDK registries — do not edit. */}



**Category:** Hyperparameters

## Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `netuid` | integer | Subnet to query. |

## CLI

```bash
btcli query difficulty --netuid <integer> --json
```

## Python

Namespace method (autocomplete, signature help):

```python
import bittensor as bt

sub = bt.Subtensor()
result = sub.hyperparameters.difficulty(netuid=1)
```

Or dispatch by name, as an agent would:

```python
result = sub.read("difficulty", netuid=1)
```

Async is the same surface awaited: `async with bt.Subtensor() as client:`.

## On-chain implementation

- Storage [`SubtensorModule.Difficulty`](/code/pallets/subtensor/src/lib.rs#L2255)

Every file is browsable under [/code](/code) exactly as built into the runtime.
