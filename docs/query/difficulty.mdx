---
title: "difficulty"
description: "Current PoW registration difficulty for a subnet."
---

{/* GENERATED by scripts/generate.py from the SDK registries — do not edit. */}



**Category:** Hyperparameters

## Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `netuid` | integer | Subnet to query. |

## CLI

```bash
btcli query difficulty --netuid <integer> --json
```

## Python

Typed namespace method (autocomplete, signature help):

```python
import bittensor as sub

async with sub.Client("finney") as client:
    result = await client.hyperparameters.difficulty(netuid=1)
```

Or dispatch by name, as an agent would:

```python
result = await client.read("difficulty", netuid=1)
```
