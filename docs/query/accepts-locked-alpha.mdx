---
title: "accepts-locked-alpha"
description: "Whether a coldkey accepts incoming locked alpha transfers."
---

{/* GENERATED by scripts/generate.py from the SDK registries — do not edit. */}

Coldkeys reject locked alpha by default; the pallet's
`set_reject_locked_alpha(false)` sets bit 0 of the coldkey's
`AccountFlags` to opt in.

**Category:** Locks & conviction

## Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `coldkey_ss58` | string | Coldkey whose locked-alpha acceptance flag to read. |

## CLI

```bash
btcli query accepts-locked-alpha --coldkey <ss58|name> --json
```

## Python

Namespace method (autocomplete, signature help):

```python
import bittensor as bt

sub = bt.Subtensor()
result = sub.locks.accepts_locked_alpha(coldkey_ss58="5F...")
```

Or dispatch by name, as an agent would:

```python
result = sub.read("accepts_locked_alpha", coldkey_ss58="5F...")
```

Async is the same surface awaited: `async with bt.Subtensor() as client:`.

## On-chain implementation

- Storage [`SubtensorModule.AccountFlags`](/code/pallets/subtensor/src/lib.rs#L1470)

Every file is browsable under [/code](/code) exactly as built into the runtime.
