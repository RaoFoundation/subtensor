---
title: "validator-basket-nav"
description: "A validator's beta basket net asset value in TAO (realizable quote)."
---

{/* GENERATED by scripts/generate.py from the SDK registries — do not edit. */}



**Category:** Staking

## Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `hotkey_ss58` | string | Validator hotkey whose basket to value. |

## CLI

```bash
btcli query validator-basket-nav --hotkey <ss58|name> --json
```

## Python

Namespace method (autocomplete, signature help):

```python
import bittensor as bt

sub = bt.Subtensor()
result = sub.staking.validator_basket_nav(hotkey_ss58="5F...")
```

Or dispatch by name, as an agent would:

```python
result = sub.read("validator_basket_nav", hotkey_ss58="5F...")
```

Async is the same surface awaited: `async with bt.Subtensor() as client:`.

## On-chain implementation

- Runtime API [`BetaBasketRuntimeApi.get_validator_basket_nav`](/code/runtime/src/lib.rs#L2360-L2362)

Every file is browsable under [/code](/code) exactly as built into the runtime.
