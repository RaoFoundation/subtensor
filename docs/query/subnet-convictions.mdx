---
title: "subnet-convictions"
description: "Every hotkey with locked stake on a subnet, rolled forward to now."
---

{/* GENERATED by scripts/generate.py from the SDK registries — do not edit. */}

Per hotkey: locked mass, conviction, and the estimated blocks until its
conviction reaches 10% of the subnet's outstanding alpha. That per-hotkey
figure is a projection heuristic, not a takeover trigger: the ownership
takeover in `change_subnet_owner_if_needed` requires the subnet to be
at least ~1 year old (2,629,800 blocks) and the total aggregate
conviction across all lockers to reach 10% of `SubnetAlphaOut`, at
which point the highest-conviction hotkey's coldkey becomes the subnet
owner. Projections assume the lock rates and alpha out stay constant.

**Category:** Locks & conviction

## Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `netuid` | integer | Subnet to query. |

## CLI

```bash
btcli query subnet-convictions --netuid <integer> --json
```

## Python

Namespace method (autocomplete, signature help):

```python
import bittensor as bt

sub = bt.Subtensor()
result = sub.locks.subnet_convictions(netuid=1)
```

Or dispatch by name, as an agent would:

```python
result = sub.read("subnet_convictions", netuid=1)
```

Async is the same surface awaited: `async with bt.Subtensor() as client:`.

## On-chain implementation

- Storage [`SubtensorModule.HotkeyLock`](/code/pallets/subtensor/src/lib.rs#L1682)
- Storage [`SubtensorModule.DecayingHotkeyLock`](/code/pallets/subtensor/src/lib.rs#L1694)
- Storage [`SubtensorModule.OwnerLock`](/code/pallets/subtensor/src/lib.rs#L1706)
- Storage [`SubtensorModule.DecayingOwnerLock`](/code/pallets/subtensor/src/lib.rs#L1710)
- Storage [`SubtensorModule.SubnetOwnerHotkey`](/code/pallets/subtensor/src/lib.rs#L2078)
- Storage [`SubtensorModule.SubnetAlphaOut`](/code/pallets/subtensor/src/lib.rs#L1514)
- Storage [`SubtensorModule.UnlockRate`](/code/pallets/subtensor/src/lib.rs#L1748)
- Storage [`SubtensorModule.MaturityRate`](/code/pallets/subtensor/src/lib.rs#L1744)
- Storage [`SubtensorModule.NetworkRegisteredAt`](/code/pallets/subtensor/src/lib.rs#L2012)

Every file is browsable under [/code](/code) exactly as built into the runtime.
