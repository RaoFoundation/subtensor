---
title: "metagraph"
description: "The full metagraph for a subnet in one call (stakes, ranks, emissions, axons, ...)."
---

{/* GENERATED by scripts/generate.py from the SDK registries — do not edit. */}

`name` and `symbol` are decoded to text (the wire carries them as
compact-u16 vectors of utf-8 bytes, unlike every other text field).

**Category:** Subnets

## Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `netuid` | integer | Subnet whose metagraph to fetch. |

## CLI

```bash
btcli query metagraph --netuid <integer> --json
```

## Python

```python
import bittensor as bt

sub = bt.Subtensor()
result = sub.read("metagraph", netuid=1)
```

## On-chain implementation

- Runtime API [`SubnetInfoRuntimeApi.get_metagraph`](/code/pallets/subtensor/src/rpc_info/metagraph.rs#L625-L793)

Every file is browsable under [/code](/code) exactly as built into the runtime.
