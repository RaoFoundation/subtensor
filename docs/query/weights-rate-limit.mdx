---
title: "weights-rate-limit"
description: "Blocks a hotkey must wait between weight sets on a subnet."
---

{/* GENERATED by scripts/generate.py from the SDK registries — do not edit. */}



**Category:** Hyperparameters

## Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `netuid` | integer | Subnet to query. |

## CLI

```bash
btcli query weights-rate-limit --netuid <integer> --json
```

## Python

Namespace method (autocomplete, signature help):

```python
import bittensor as bt

sub = bt.Subtensor()
result = sub.hyperparameters.weights_rate_limit(netuid=1)
```

Or dispatch by name, as an agent would:

```python
result = sub.read("weights_rate_limit", netuid=1)
```

Async is the same surface awaited: `async with bt.Subtensor() as client:`.

## On-chain implementation

- Storage [`SubtensorModule.WeightsSetRateLimit`](/code/pallets/subtensor/src/lib.rs#L2097)

Every file is browsable under [/code](/code) exactly as built into the runtime.
