---
title: "weights-rate-limit"
description: "Blocks a hotkey must wait between weight sets on a subnet."
---

{/* GENERATED by scripts/generate.py from the SDK registries — do not edit. */}



**Category:** Hyperparameters

## Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `netuid` | integer | Subnet to query. |

## CLI

```bash
btcli query weights-rate-limit --netuid <integer> --json
```

## Python

Typed namespace method (autocomplete, signature help):

```python
import bittensor as sub

async with sub.Client("finney") as client:
    result = await client.hyperparameters.weights_rate_limit(netuid=1)
```

Or dispatch by name, as an agent would:

```python
result = await client.read("weights_rate_limit", netuid=1)
```
