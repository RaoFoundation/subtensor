---
title: "derivative-position"
description: "One open position for a coldkey on a subnet and side, or None."
---

{/* GENERATED by scripts/generate.py from the SDK registries — do not edit. */}

`cushion` is the TAO the owner put up. `proceeds`, `debt`, and `escrow` are
the position's `legs`, each already in
its own token: a short holds TAO proceeds and TAO escrow and owes alpha; a
long holds alpha proceeds and alpha escrow and owes TAO. `fee_per_day_tao`
was fixed at open; `accrued_fee_tao` is what would be charged if closed now.

**Category:** Prices & swaps

## Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `coldkey_ss58` | string | Coldkey that owns the position. |
| `netuid` | integer | Subnet the position is on. |
| `side` | string | `Short` or `Long`. |

## CLI

```bash
btcli query derivative-position --coldkey <ss58|name> --netuid <integer> --side <string> --json
```

## Python

Namespace method (autocomplete, signature help):

```python
import bittensor as bt

sub = bt.Subtensor()
result = sub.prices.derivative_position(coldkey_ss58="5F...", netuid=1, side="...")
```

Or dispatch by name, as an agent would:

```python
result = sub.read("derivative_position", coldkey_ss58="5F...", netuid=1, side="...")
```

Async is the same surface awaited: `async with bt.Subtensor() as client:`.

## On-chain implementation

- Storage [`Derivatives.Positions`](/code/pallets/derivatives/src/lib.rs#L82)

Every file is browsable under [/code](/code) exactly as built into the runtime.
