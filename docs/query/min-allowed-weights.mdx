---
title: "min-allowed-weights"
description: "Minimum number of weights a validator must set on a subnet. A pure self-weight (a single entry pointing at the caller's own uid) bypasses the minimum, and the subnet owner bypasses validator-permit rules."
---

{/* GENERATED by scripts/generate.py from the SDK registries — do not edit. */}



**Category:** Hyperparameters

## Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `netuid` | integer | Subnet to query. |

## CLI

```bash
btcli query min-allowed-weights --netuid <integer> --json
```

## Python

Namespace method (autocomplete, signature help):

```python
import bittensor as bt

sub = bt.Subtensor()
result = sub.hyperparameters.min_allowed_weights(netuid=1)
```

Or dispatch by name, as an agent would:

```python
result = sub.read("min_allowed_weights", netuid=1)
```

Async is the same surface awaited: `async with bt.Subtensor() as client:`.

## On-chain implementation

- Storage [`SubtensorModule.MinAllowedWeights`](/code/pallets/subtensor/src/lib.rs#L2257)

Every file is browsable under [/code](/code) exactly as built into the runtime.
