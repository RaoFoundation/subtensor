---
title: "is-delegate"
description: "Whether a hotkey is a delegate."
---

{/* GENERATED by scripts/generate.py from the SDK registries — do not edit. */}



**Category:** Delegation

## Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `hotkey_ss58` | string | Hotkey to check for delegate status. |

## CLI

```bash
btcli query is-delegate --hotkey <ss58|name> --json
```

## Python

Namespace method (autocomplete, signature help):

```python
import bittensor as bt

sub = bt.Subtensor()
result = sub.delegation.is_delegate(hotkey_ss58="5F...")
```

Or dispatch by name, as an agent would:

```python
result = sub.read("is_delegate", hotkey_ss58="5F...")
```

Async is the same surface awaited: `async with bt.Subtensor() as client:`.
