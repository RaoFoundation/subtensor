---
title: "subnet-emission-enabled"
description: "Root-controlled pool-side TAO emission flag for a subnet (1 = enabled). When 0, the subnet earns no TAO emission share even while subnet_is_active is true; only root can flip it (see the set_subnet_emission_enabled intent)."
---

{/* GENERATED by scripts/generate.py from the SDK registries — do not edit. */}



**Category:** Hyperparameters

## Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `netuid` | integer | Subnet to query. |

## CLI

```bash
btcli query subnet-emission-enabled --netuid <integer> --json
```

## Python

Namespace method (autocomplete, signature help):

```python
import bittensor as bt

sub = bt.Subtensor()
result = sub.hyperparameters.subnet_emission_enabled(netuid=1)
```

Or dispatch by name, as an agent would:

```python
result = sub.read("subnet_emission_enabled", netuid=1)
```

Async is the same surface awaited: `async with bt.Subtensor() as client:`.

## On-chain implementation

- Storage [`SubtensorModule.SubnetEmissionEnabled`](/code/pallets/subtensor/src/lib.rs#L1658)

Every file is browsable under [/code](/code) exactly as built into the runtime.
