---
title: "derivative-positions-on-subnet"
description: "Every open position on a subnet, whoever owns it. Same fields as"
---

{/* GENERATED by scripts/generate.py from the SDK registries — do not edit. */}

`derivative_position`.

The list a liquidator works from: filter on `healthy` being False or
`expired` being True and call `close_derivative` with that `coldkey` as
`owner_ss58`.

**Category:** Prices & swaps

## Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `netuid` | integer | Subnet whose open positions to list. |

## CLI

```bash
btcli query derivative-positions-on-subnet --netuid <integer> --json
```

## Python

Namespace method (autocomplete, signature help):

```python
import bittensor as bt

sub = bt.Subtensor()
result = sub.prices.derivative_positions_on_subnet(netuid=1)
```

Or dispatch by name, as an agent would:

```python
result = sub.read("derivative_positions_on_subnet", netuid=1)
```

Async is the same surface awaited: `async with bt.Subtensor() as client:`.

## On-chain implementation

- Storage [`Derivatives.OpenByNetuid`](/code/pallets/derivatives/src/lib.rs#L100)
- Storage [`Derivatives.Positions`](/code/pallets/derivatives/src/lib.rs#L88)

Every file is browsable under [/code](/code) exactly as built into the runtime.
