---
title: "balances"
description: "Free TAO balance for several coldkey addresses in one batched request."
---

{/* GENERATED by scripts/generate.py from the SDK registries — do not edit. */}



**Category:** Accounts & keys

## Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `coldkey_ss58s` | array | Coldkeys whose free balances to read. |

## CLI

```bash
btcli query balances --coldkey-ss58s <array> --json
```

## Python

Typed namespace method (autocomplete, signature help):

```python
import bittensor as sub

async with sub.Client("finney") as client:
    result = await client.balances.balances(coldkey_ss58s=[...])
```

Or dispatch by name, as an agent would:

```python
result = await client.read("balances", coldkey_ss58s=[...])
```
