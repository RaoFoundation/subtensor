---
title: "auto-stake-all"
description: "Every auto-stake destination configured for a coldkey."
---

{/* GENERATED by scripts/generate.py from the SDK registries — do not edit. */}

One entry per subnet where a destination hotkey is set; subnets with no
entry have auto-stake unset.

**Category:** Staking

## Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `coldkey_ss58` | string | Coldkey whose auto-stake destinations to list. |

## CLI

```bash
btcli query auto-stake-all --coldkey <ss58|name> --json
```

## Python

Typed namespace method (autocomplete, signature help):

```python
import bittensor as sub

async with sub.Client("finney") as client:
    result = await client.staking.auto_stake_all(coldkey_ss58="5F...")
```

Or dispatch by name, as an agent would:

```python
result = await client.read("auto_stake_all", coldkey_ss58="5F...")
```
