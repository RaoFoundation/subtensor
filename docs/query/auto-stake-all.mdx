---
title: "auto-stake-all"
description: "Every auto-stake destination configured for a coldkey."
---

{/* GENERATED by scripts/generate.py from the SDK registries — do not edit. */}

One entry per subnet where a destination hotkey is set; subnets with no
entry have auto-stake unset.

**Category:** Staking

## Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `coldkey_ss58` | string | Coldkey whose auto-stake destinations to list. |

## CLI

```bash
btcli query auto-stake-all --coldkey <ss58|name> --json
```

## Python

Namespace method (autocomplete, signature help):

```python
import bittensor as bt

sub = bt.Subtensor()
result = sub.staking.auto_stake_all(coldkey_ss58="5F...")
```

Or dispatch by name, as an agent would:

```python
result = sub.read("auto_stake_all", coldkey_ss58="5F...")
```

Async is the same surface awaited: `async with bt.Subtensor() as client:`.

## On-chain implementation

- Storage [`SubtensorModule.AutoStakeDestination`](/code/pallets/subtensor/src/lib.rs#L1508)

Every file is browsable under [/code](/code) exactly as built into the runtime.
