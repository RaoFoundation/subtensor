---
title: "hotkey-owner"
description: "The coldkey that owns a hotkey, or None if unowned."
---

{/* GENERATED by scripts/generate.py from the SDK registries — do not edit. */}



**Category:** Neurons & registration

## Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `hotkey_ss58` | string | Hotkey whose owning coldkey to look up. |

## CLI

```bash
btcli query hotkey-owner --hotkey <ss58|name> --json
```

## Python

Namespace method (autocomplete, signature help):

```python
import bittensor as bt

sub = bt.Subtensor()
result = sub.neurons.hotkey_owner(hotkey_ss58="5F...")
```

Or dispatch by name, as an agent would:

```python
result = sub.read("hotkey_owner", hotkey_ss58="5F...")
```

Async is the same surface awaited: `async with bt.Subtensor() as client:`.

## On-chain implementation

- Storage [`SubtensorModule.Owner`](/code/pallets/subtensor/src/lib.rs#L1289)

Every file is browsable under [/code](/code) exactly as built into the runtime.
