---
title: "OwnerOnly"
description: "Sign with the key or origin that owns the target object, then retry"
---

{/* GENERATED by scripts/generate.py from the SDK registries — do not edit. */}

The position's equity still covers one day of rent at the chain's quote, so only its owner may close it. Check `healthy` and `equity_tao` in `derivative-position`; the SDK estimate can differ from the chain's Balancer quote near the line.

Declared by the `Derivatives` pallet; it classifies to the semantic code [`not_authorized`](/docs/errors/not-authorized).

Declared at [`pallets/derivatives/src/lib.rs#L229`](/code/pallets/derivatives/src/lib.rs#L229).

## Remediation

Sign with the key or origin that owns the target object, then retry

The same explanation is available in the terminal: `btcli explain OwnerOnly`.
