---
title: "NotEnoughBalanceToPaySwapHotKey"
description: "Fund the signing account or reduce the amount; check with `btcli wallet balance`"
---

{/* GENERATED by scripts/generate.py from the SDK registries — do not edit. */}

The coldkey's free TAO balance is below the hotkey swap cost (a per-subnet cost applies when swapping on a single netuid). Check `btcli wallet balance` against the key swap cost and fund the coldkey before retrying.

Declared by the `SubtensorModule` pallet; it classifies to the semantic code [`insufficient_balance`](/docs/errors/insufficient-balance).

Declared at [`pallets/subtensor/src/macros/errors.rs#L114`](/code/pallets/subtensor/src/macros/errors.rs#L114).

## Remediation

Fund the signing account or reduce the amount; check with `btcli wallet balance`

The same explanation is available in the terminal: `btcli explain NotEnoughBalanceToPaySwapHotKey`.
