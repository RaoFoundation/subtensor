---
title: "NotEnoughStake"
description: "Fund the signing account or reduce the amount; check with `btcli wallet balance`"
---

{/* GENERATED by scripts/generate.py from the SDK registries — do not edit. */}

The caller's hotkey holds less stake than the action requires; a generic insufficient-stake failure on staking-related calls. Check the hotkey's stake on the relevant subnet, e.g. `btcli stake list`, against the amount the extrinsic needs.

Declared by the `SubtensorModule` pallet; it classifies to the semantic code [`insufficient_balance`](/docs/errors/insufficient-balance).

Declared at [`pallets/subtensor/src/macros/errors.rs#L29`](/code/pallets/subtensor/src/macros/errors.rs#L29).

## Remediation

Fund the signing account or reduce the amount; check with `btcli wallet balance`

The same explanation is available in the terminal: `btcli explain NotEnoughStake`.
