[package]
name = "pallet-usd-psm-rpc"
publish = false
version = "0.1.0"
description = "RPC interface for the canonical rails (USD PSM pallet)"
edition.workspace = true

[lints]
workspace = true

[dependencies]
jsonrpsee = { workspace = true, features = ["client-core", "macros", "server"] }
pallet-usd-psm-runtime-api = { path = "../runtime-api" }
sp-api.workspace = true
sp-blockchain.workspace = true
sp-core.workspace = true
sp-runtime.workspace = true
