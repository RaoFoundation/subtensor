[profile.default]
src = "src"
out = "out"
libs = ["lib"]
test = "test"
script = "script"
solc_version = "0.8.24"
# Bittensor EVM (Frontier) does not support post-London opcodes like PUSH0.
evm_version = "london"
optimizer = true
optimizer_runs = 200

[fmt]
line_length = 100
tab_width = 4
