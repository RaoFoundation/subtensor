"""Metadata-driven code generator for the Bittensor SDK.

Reads the runtime's own metadata and emits the mechanical wire layer under
``bittensor/_generated/``. Generated files are committed and CI-checked; nothing
here runs at SDK runtime.
"""
